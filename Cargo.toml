[package]
name = "deparch"
version = "0.1.0"
edition = "2021"
description = "Cross-language dependency archaeology: why is this package here, and does anything actually use it?"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
anyhow = "1"
